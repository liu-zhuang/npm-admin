class Repository {
	constructor (code, name, type, git, desc, author) {
		this.RepoCode = code;
		this.RepoName = name;
		this.RepoType = type;
		this.RepoGit = git;
		this.RepoDesc = desc;
		this.RepoAuthor = author;
	}

	CreateRepo () {
	}

	QueryAllRepo () {
	}
}